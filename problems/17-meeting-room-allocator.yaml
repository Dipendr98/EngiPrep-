id: 17
title: Meeting Room Allocator
category: scheduling
difficulty: Medium
summary: Find the minimum rooms needed and allocate meetings to rooms.
description: 'Given a list of meetings with [start, end] times, determine the minimum number of rooms required. Additionally, return the room assignments: which meeting goes in which room. Rooms should be reused when possible.'
scenario: ''
constraints: []
examples: []
starter_code: |-
  def allocate_meeting_rooms(meetings: list[list[int]]) -> dict:
      pass
follow_ups:
- Add room capacities and meeting sizes.
- Find all free slots in a given room.
- Support recurring meetings.
key_skills:
- sorting
- heap/priority queue
- interval scheduling
- greedy
test_type: function
test_cases:
- label: room reuse
  input:
    meetings:
    - - 0
      - 30
    - - 5
      - 10
    - - 15
      - 20
  expected:
    room_count: 2
    assignments:
    - 0
    - 1
    - 1
- label: single room
  input:
    meetings:
    - - 1
      - 2
    - - 2
      - 3
    - - 3
      - 4
  expected:
    room_count: 1
    assignments:
    - 0
    - 0
    - 0
function_name: allocate_meeting_rooms
explanation: |-
  This problem is mainly about choosing a clean invariant around **sorting** and then preserving it on every operation.

  A strong solution starts by deciding what state to keep in memory, how each public operation updates that state, and what the time complexity should be in the common case.

  The usual tradeoff here is how **heap/priority queue** supports the fast path without making updates or edge cases harder to reason about.

  When studying it, write down the data structure layout first, then walk through the smallest tricky example by hand: duplicate updates, missing keys, empty input, or a boundary timestamp/index.

  A polished interview answer should also call out complexity, edge cases, and the invariant that proves the implementation stays correct after each step.
references:
- Model which item should be processed next and which data structure answers that question most directly.
- Keep one eye on tie-breaking rules so your output stays deterministic.
- After implementing Meeting Room Allocator, re-run the provided hardcoded tests and explain why each one protects an important invariant.
